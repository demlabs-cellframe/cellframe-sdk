# Механизм блокировки UTXO - Примеры использования

## Обзор

Механизм блокировки UTXO (Unspent Transaction Output - неизрасходованных выходов транзакций) позволяет эмитентам токенов предотвращать траты конкретных UTXO. Эта функция **включена по умолчанию** для всех CF20 токенов и управляется через флаги токенов и команды `token_update`.

## Содержание

1. [Базовое использование](#базовое-использование)
2. [Отложенная активация и разблокировка](#отложенная-активация-и-разблокировка)
3. [Управление флагами](#управление-флагами)
4. [Интеграция с адресной блокировкой](#интеграция-с-адресной-блокировкой)
5. [Типичные сценарии использования](#типичные-сценарии-использования)
6. [Лучшие практики](#лучшие-практики)
7. [Арбитражные транзакции](#10-арбитражные-транзакции)

---

## Базовое использование

### 1. Создание токена с включенной блокировкой UTXO (по умолчанию)

Блокировка UTXO **включена по умолчанию** для всех CF20 токенов. Специальные флаги не требуются:

```bash
# Создание CF20 токена с включенной блокировкой UTXO (поведение по умолчанию)
cellframe-node-cli token_decl \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -total_supply 1000000 \
    -decimals 18 \
    -signs_total 1 \
    -signs_emission 1 \
    -certs owner_cert
```

### 2. Блокировка конкретного UTXO (немедленно)

```bash
# Заблокировать UTXO немедленно
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -utxo_blocked_add 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef:0 \
    -certs owner_cert
```

**Формат:** `<tx_hash>:<out_idx>`

- `tx_hash`: 32-байтовый хеш транзакции (64 hex символа с префиксом `0x`)
- `out_idx`: Индекс выхода (целое число начиная с 0)

### 3. Разблокировка UTXO (немедленно)

```bash
# Разблокировать UTXO немедленно (удалить из списка блокировки)
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -utxo_blocked_remove 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef:0 \
    -certs owner_cert
```

### 4. Очистка всех заблокированных UTXO

```bash
# Очистить весь список блокировки UTXO
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -utxo_blocked_clear \
    -certs owner_cert
```

### 5. Просмотр списка блокировки UTXO

```bash
# Показать информацию о токене включая список блокировки UTXO
cellframe-node-cli token info -net mynetwork -name TEST
```

**Пример вывода:**
```json
{
  "ticker": "TEST",
  "type": "CF20",
  "flags": "0x00000000",
  "utxo_blocklist_count": 2,
  "utxo_blocklist": [
    {
      "tx_hash": "0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef",
      "out_idx": 0,
      "blocked_time": 1697529600,
      "becomes_effective": 1697529600,
      "becomes_unblocked": 0
    },
    {
      "tx_hash": "0xfedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321",
      "out_idx": 1,
      "blocked_time": 1697530000,
      "becomes_effective": 1697533600,
      "becomes_unblocked": 1697540800
    }
  ]
}
```

**Описание полей:**
- `blocked_time`: Когда UTXO был добавлен в список блокировки (Unix timestamp)
- `becomes_effective`: Когда блокировка активируется (blockchain time)
- `becomes_unblocked`: Когда блокировка истекает (`0` = постоянно)

---

## Отложенная активация и разблокировка

Механизм блокировки UTXO поддерживает временную семантику через привязку к blockchain time.

### 1. Отложенная активация блокировки

Заблокировать UTXO, но активация происходит только после указанного blockchain time:

```bash
# Заблокировать UTXO начиная с timestamp 1700000000 (blockchain time)
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -utxo_blocked_add 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef:0:1700000000 \
    -certs owner_cert
```

**Формат:** `<tx_hash>:<out_idx>:<timestamp>`

**Поведение:**
- UTXO остается доступным для траты до `blockchain_time >= 1700000000`
- После активации UTXO становится заблокированным и не может быть потрачен
- Полезно для запланированных ограничений (например, периоды вестинга)

### 2. Отложенная разблокировка (автоматическое истечение)

Временно заблокировать UTXO - он автоматически разблокируется после указанного времени:

```bash
# Заблокировать UTXO сейчас, но авто-разблокировать в timestamp 1700100000
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -utxo_blocked_remove 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef:0:1700100000 \
    -certs owner_cert
```

**Формат:** `<tx_hash>:<out_idx>:<timestamp>`

**Поведение:**
- Если UTXO в данный момент заблокирован, он остается заблокированным до `blockchain_time >= 1700100000`
- После истечения UTXO автоматически становится доступным для траты
- Запись удаляется из списка блокировки после истечения
- Полезно для временных ограничений (например, периоды lock-up)

### 3. Комбинированный пример: Период вестинга

```bash
# Шаг 1: Заблокировать UTXO немедленно для вестинга
cellframe-node-cli token_update \
    -net mynetwork \
    -token VEST \
    -type CF20 \
    -utxo_blocked_add 0xabcd...1234:0 \
    -certs owner_cert

# Шаг 2: Запланировать автоматическую разблокировку через 6 месяцев (timestamp: 1715788800)
cellframe-node-cli token_update \
    -net mynetwork \
    -token VEST \
    -type CF20 \
    -utxo_blocked_remove 0xabcd...1234:0:1715788800 \
    -certs owner_cert
```

**Результат:**
- UTXO заблокирован немедленно
- UTXO автоматически становится доступным для траты после 15 мая 2024
- Дальнейшее ручное вмешательство не требуется

---

## Управление флагами

### 1. Отключение блокировки UTXO (Opt-Out)

Если вы хотите полностью отключить блокировку UTXO для токена:

```bash
# Создать токен с отключенной блокировкой UTXO
cellframe-node-cli token_decl \
    -net mynetwork \
    -token NOBLOCK \
    -type CF20 \
    -total_supply 1000000 \
    -decimals 18 \
    -signs_total 1 \
    -signs_emission 1 \
    -flags UTXO_BLOCKING_DISABLED \
    -certs owner_cert
```

Или отключить позже через `token_update`:

```bash
# Отключить блокировку UTXO для существующего токена
cellframe-node-cli token_update \
    -net mynetwork \
    -token TEST \
    -type CF20 \
    -flag_set UTXO_BLOCKING_DISABLED \
    -certs owner_cert
```

**Эффект:**
- Все UTXO становятся доступными для траты независимо от списка блокировки
- Данные списка блокировки сохраняются, но игнорируются
- Может быть повторно включен снятием флага

### 2. Создание неизменяемого списка блокировки UTXO

Используйте `UTXO_STATIC_BLOCKLIST` для предотвращения любых дальнейших изменений списка блокировки:

```bash
# Создать токен со статическим (неизменяемым) списком блокировки UTXO
cellframe-node-cli token_decl \
    -net mynetwork \
    -token STATIC \
    -type CF20 \
    -total_supply 1000000 \
    -decimals 18 \
    -signs_total 1 \
    -signs_emission 1 \
    -flags UTXO_STATIC_BLOCKLIST \
    -utxo_blocked_add 0xabcd...1234:0 \
    -utxo_blocked_add 0xef01...5678:1 \
    -certs owner_cert
```

**Эффект:**
- Список блокировки UTXO устанавливается при создании токена
- Все последующие операции `token_update` с `-utxo_blocked_add`, `-utxo_blocked_remove` или `-utxo_blocked_clear` **отклоняются**
- Список блокировки становится постоянным и неизменяемым
- **Предупреждение:** Это односторонняя операция, которую нельзя отменить

---

## Интеграция с адресной блокировкой

Блокировка UTXO и адресная блокировка - **независимые** функции, которые можно использовать вместе для детального контроля.

### Пример: Гибридный контроль

```bash
# Шаг 1: Создать токен с блокировкой UTXO и адресной блокировкой
cellframe-node-cli token_decl \
    -net mynetwork \
    -token HYBRID \
    -type CF20 \
    -total_supply 1000000 \
    -decimals 18 \
    -signs_total 1 \
    -signs_emission 1 \
    -tx_sender_blocked 0x00...bad_actor \
    -certs owner_cert

# Шаг 2: Заблокировать конкретный UTXO от хорошего адреса (например, эскроу)
cellframe-node-cli token_update \
    -net mynetwork \
    -token HYBRID \
    -type CF20 \
    -utxo_blocked_add 0xabcd...escrow_tx:0 \
    -certs owner_cert
```

**Результат:**
- Адрес `0x00...bad_actor` не может отправлять HYBRID токены (адресная блокировка)
- UTXO `0xabcd...escrow_tx:0` не может быть потрачен даже если принадлежит разрешенному адресу (блокировка UTXO)
- Все остальные UTXO от незаблокированных адресов доступны для траты

---

## Типичные сценарии использования

### 1. Вестинг / Периоды блокировки

```bash
# Заблокировать распределение для команды на 12 месяцев
cellframe-node-cli token_update \
    -net mynetwork \
    -token COMPANY \
    -type CF20 \
    -utxo_blocked_add 0xteam_allocation_tx:0 \
    -certs owner_cert

# Запланировать авто-разблокировку через 12 месяцев
cellframe-node-cli token_update \
    -net mynetwork \
    -token COMPANY \
    -type CF20 \
    -utxo_blocked_remove 0xteam_allocation_tx:0:1733097600 \
    -certs owner_cert
```

### 2. Эскроу-сервисы

```bash
# Заблокировать эскроу UTXO до разрешения спора
cellframe-node-cli token_update \
    -net mynetwork \
    -token TRADE \
    -type CF20 \
    -utxo_blocked_add 0xescrow_tx:0 \
    -certs escrow_cert

# Освободить эскроу после разрешения
cellframe-node-cli token_update \
    -net mynetwork \
    -token TRADE \
    -type CF20 \
    -utxo_blocked_remove 0xescrow_tx:0 \
    -certs escrow_cert
```

### 3. Реагирование на инциденты безопасности

```bash
# Экстренная ситуация: Заблокировать подозрительный UTXO
cellframe-node-cli token_update \
    -net mynetwork \
    -token SEC \
    -type CF20 \
    -utxo_blocked_add 0xsuspicious_tx:0 \
    -certs security_cert

# Провести расследование и либо:
# а) Разблокировать если ложное срабатывание
cellframe-node-cli token_update \
    -net mynetwork \
    -token SEC \
    -type CF20 \
    -utxo_blocked_remove 0xsuspicious_tx:0 \
    -certs security_cert

# б) Оставить заблокированным постоянно (действий не требуется, becomes_unblocked = 0)
```

---

## 10. Арбитражные транзакции

### Обзор

**Арбитражные транзакции** - это специальные аварийные транзакции, которые позволяют владельцам токенов обходить **ВСЕ** механизмы блокировки (блокировка UTXO, условные выходы, баны адресов) для восстановления средств или разрешения споров. Они помечаются специальным TSD маркером (`DAP_CHAIN_TX_TSD_TYPE_ARBITRAGE = 0x00A1`) и имеют строгие правила валидации.

### Назначение

Арбитражные транзакции предназначены для экстренных ситуаций:
- Восстановление средств из заблокированных UTXO
- Разрешение споров между сторонами
- Экстренное вмешательство владельца токена
- Соответствие судебным решениям или регуляторным требованиям

### Модель безопасности

⚠️ **КРИТИЧЕСКИЕ ОГРАНИЧЕНИЯ БЕЗОПАСНОСТИ:**

1. **Должна быть подписана владельцем токена** - Требуется валидная подпись из `auth_pkeys` токена
2. **Минимальный порог подписей** - Должно быть выполнено требование `auth_signs_valid`:
   - Подпись кошелька считается как 1
   - Дополнительные подписи из параметра `-certs`
   - Общее количество подписей должно быть >= `auth_signs_valid`
   - Если недостаточно, транзакция остается в mempool для дополнительных подписей
3. **ТОЛЬКО адрес сетевых комиссий** - Может отправлять средства **ТОЛЬКО** на адрес сетевых комиссий (`net->pub.fee_addr`), а НЕ в список `tx_recv_allow`
4. **Может быть отключена** - Токен может установить флаг `UTXO_ARBITRAGE_TX_DISABLED` для постоянного отключения арбитража
5. **Поддержка распределенного подписания** - Используйте команду `tx_sign` для добавления подписей с разных узлов

🔒 **Почему только адрес сетевых комиссий?** Это предотвращает добавление владельцами токенов своих собственных адресов в `tx_recv_allow` и неправомерное использование арбитража. Адрес сетевых комиссий контролируется операторами сети, а не отдельными владельцами токенов.

### Использование

#### Создание арбитражной транзакции

```bash
# Создание арбитражной транзакции с флагом -arbitrage
# ПРИМЕЧАНИЕ: параметр -to_addr НЕ требуется для арбитражных транзакций
# CLI автоматически отправляет ВСЕ выходы на адрес сетевых комиссий (net->pub.fee_addr)
cellframe-node-cli tx_create \
    -net mynetwork \
    -token MYTOKEN \
    -from_wallet owner_wallet \
    -value 1000.0 \
    -fee 0.01 \
    -arbitrage \
    -certs token_owner_cert
```

**Предварительные требования:**
1. Сеть должна иметь настроенный `fee_addr` (адрес сбора сетевых комиссий)
2. Арбитраж не должен быть отключен (флаг `UTXO_ARBITRAGE_TX_DISABLED` не установлен)
3. Транзакция должна быть подписана сертификатом(-ами) владельца токена

⚠️ **АВТОМАТИЧЕСКАЯ ОБРАБОТКА АДРЕСОВ:** Арбитражные транзакции автоматически отправляют все выходы на адрес сетевых комиссий (`net->pub.fee_addr`):
- Параметр `-to_addr` опционален и будет игнорироваться если указан
- Все выходы (основной, сдача, сдача комиссии) идут только на адрес комиссий
- Адрес комиссий настраивается на уровне сети, а не владельцами токенов

#### Проверка адреса сетевых комиссий

Перед использованием арбитража проверьте адрес сетевых комиссий:

```bash
# Проверить адрес сетевых комиссий
cellframe-node-cli net get -net mynetwork fee_addr
# Вывод: mXyZ123... (адрес сбора сетевых комиссий)
```

#### Отключение арбитража (необратимо)

```bash
# Постоянно отключить арбитраж для этого токена
cellframe-node-cli token_update \
    -net mynetwork \
    -chain main \
    -token MYTOKEN \
    -flag_set UTXO_ARBITRAGE_TX_DISABLED \
    -certs token_owner_cert
```

⚠️ **Предупреждение:** После установки `UTXO_ARBITRAGE_TX_DISABLED` его нельзя снять (необратимый флаг).

### Правила валидации

Когда ledger обрабатывает арбитражную транзакцию, выполняются следующие проверки:

1. **Проверка TSD маркера** - TX должна иметь TSD `DAP_CHAIN_TX_TSD_TYPE_ARBITRAGE` (0x00A1)
2. **Проверка флага** - У токена не должен быть установлен `UTXO_ARBITRAGE_TX_DISABLED`
3. **Проверка подписей** - TX должна быть подписана:
   - **Владельцем эмиссии** (для авторизации траты UTXO)
   - **Владельцем(-ами) токена** (для авторизации арбитража - должны быть в `auth_pkeys` и соответствовать порогу `auth_signs_valid`):
     - Подпись кошелька считается как 1
     - Дополнительные подписи из параметра `-certs`
     - Общее количество валидных подписей владельцев должно быть >= `auth_signs_valid`
     - Если недостаточно (`auth_signs_valid` не достигнут), транзакция возвращает `DAP_LEDGER_CHECK_NOT_ENOUGH_VALID_SIGNS` и **остается в mempool**
4. **Проверка адреса сетевых комиссий** - Сеть должна иметь настроенный `fee_addr`
5. **Проверка адресов выходов** - **ВСЕ** выходы TX должны идти **ТОЛЬКО** на адрес сетевых комиссий (`net->pub.fee_addr`)

⚠️ **КРИТИЧНО:** Арбитражные транзакции могут отправлять средства ТОЛЬКО на адрес сбора сетевых комиссий.

**Поведение mempool:**
- Транзакции с недостаточными подписями (`DAP_CHAIN_CS_VERIFY_CODE_NOT_ENOUGH_SIGNS`) **НЕ удаляются** из mempool
- Это позволяет распределенное подписание: транзакция может быть создана на одном узле, затем подписи добавлены с других узлов используя команду `tx_sign`
- Транзакция остается в mempool пока не наберется достаточно подписей или пока не будет удалена вручную
- После сбора `auth_signs_valid` подписей транзакция может быть обработана нормально

Если какая-либо проверка провалена (кроме недостаточных подписей), транзакция отклоняется с `DAP_LEDGER_TX_CHECK_ARBITRAGE_NOT_AUTHORIZED`.

### Обходимые проверки

Арбитражные транзакции **обходят ВСЕ** эти проверки:
- ✅ Блокировка UTXO (может тратить заблокированные UTXO)
- ✅ Условные выходы (может тратить заблокированные/условные выходы)
- ✅ Блокировка адреса отправителя (`tx_send_block` игнорируется)
- ✅ Блокировка адреса получателя (`tx_recv_block` игнорируется)

### Коды ошибок

| Код ошибки | Сообщение об ошибке | Причина |
|-----------|---------------------|---------|
| `DAP_LEDGER_TX_CHECK_ARBITRAGE_NOT_AUTHORIZED` | Арбитражная TX не авторизована | Отсутствуют подписи владельцев, отключен арбитраж, вывод на недопустимый адрес, или адрес сетевых комиссий не настроен |

### Типичные сценарии

#### Сценарий 4: Требования множественных подписей

Если токен требует множественных подписей владельцев (`auth_signs_valid > 1`), арбитражная транзакция должна быть подписана как минимум таким количеством владельцев токена.

**Важно:** Правила подсчета подписей зависят от того, **fee токен == арбитражный токен**:

##### Случай 1: fee_token == arbitrage_token (один и тот же токен)

Когда передаваемый токен совпадает с native ticker, используемым для комиссий:

- **Подпись кошелька СЧИТАЕТСЯ** для авторизации арбитража
- Нужно `(auth_signs_valid - 1)` дополнительных сертификатов через `-certs`
- Пример: Если `auth_signs_valid = 3`, нужно кошелек + 2 сертификата = 3 всего

```bash
# Токен требует 3 подписи владельцев (auth_signs_valid = 3)
# fee_token == arbitrage_token (например, оба native ticker)
cellframe-node-cli tx_create \
    -net mynetwork \
    -token MYTOKEN \
    -from_wallet owner_wallet \  # Подпись считается для арбитража (1)
    -value 1000.0 \
    -fee 0.01 \
    -arbitrage \
    -certs token_owner_cert1,token_owner_cert2  # 2 сертификата (всего = 3)
# ПРИМЕЧАНИЕ: -to_addr не нужен - автоматически отправляется на адрес комиссий
```

##### Случай 2: fee_token != arbitrage_token (разные токены)

Когда передаваемый токен отличается от native ticker, используемого для комиссий:

- **Подпись кошелька НЕ СЧИТАЕТСЯ** для авторизации арбитража (используется ТОЛЬКО для оплаты комиссии)
- Нужно **ВСЕ** `auth_signs_valid` сертификатов через `-certs`
- Пример: Если `auth_signs_valid = 3`, нужно 3 сертификата (подпись кошелька только для комиссии)

```bash
# Токен требует 3 подписи владельцев (auth_signs_valid = 3)
# ПРИМЕЧАНИЕ: Для арбитража с -from_wallet кошелек должен содержать ОБА токена:
#             - Арбитражный токен (ARBMULTI) для перевода
#             - Native токен (для оплаты комиссии)
cellframe-node-cli tx_create \
    -net mynetwork \
    -token ARBMULTI \
    -from_wallet arbitrage_wallet \
    -value 1000.0 \
    -fee 0.000000000000000001 \
    -arbitrage \
    -certs owner_cert1  # Только 1 сертификат указан (нужно 3 для auth_signs_valid=3)
# ПРЕДУПРЕЖДЕНИЕ: Транзакция создана с НЕДОСТАТОЧНЫМИ подписями (1 из 3)
# Останется в mempool пока остальные подписи не будут добавлены через tx_sign
```

##### Создание мульти-подписной арбитражной транзакции (распределенное подписание)

Когда владельцы токенов находятся на разных узлах или вы не можете предоставить все подписи сразу, создайте транзакцию с частичными подписями сначала:

**Шаг 1:** Создание арбитражной транзакции с начальными подписями:

```bash
# Узел 1: Создание арбитражной TX с недостаточными подписями
# Токен требует 3 подписи владельцев (auth_signs_valid = 3)
# ПРИМЕЧАНИЕ: Кошелек должен содержать оба токена:
#             - Арбитражный токен (ARBMULTI) для перевода  
#             - Native токен (для оплаты комиссии)
cellframe-node-cli tx_create \
    -net mynetwork \
    -token ARBMULTI \
    -from_wallet arbitrage_wallet \
    -value 10000.0 \
    -fee 0.000000000000000001 \
    -arbitrage \
    -certs owner_cert1  # Только 1 сертификат владельца (нужно еще 2 для auth_signs_valid=3)
# Уведомление CLI: "Транзакция будет создана и помещена в mempool. 
#                   Добавьте оставшиеся 2 подписи через команду 'tx_sign'."
# Возвращает хеш транзакции: 0xABC123...
```

**Шаг 2:** Добавление второй подписи с другого узла:

```bash
# Узел 2: Добавление подписи от второго владельца
cellframe-node-cli tx_sign \
    -net mynetwork \
    -chain main \
    -tx 0xABC123... \
    -certs owner_cert2
# Возвращает НОВЫЙ хеш транзакции: 0xDEF456... (хеш меняется после добавления подписи)
# Теперь имеет 2 подписи владельцев (еще 1 нужна)
```

**Шаг 3:** Добавление третьей подписи для завершения авторизации:

```bash
# Узел 3: Добавление подписи от третьего владельца
cellframe-node-cli tx_sign \
    -net mynetwork \
    -chain main \
    -tx 0xDEF456... \  # Использовать НОВЫЙ хеш из Шага 2
    -certs owner_cert3
# Возвращает ФИНАЛЬНЫЙ хеш транзакции: 0x789ABC...
# Теперь имеет 3 подписи владельцев - достаточно для обработки
# Транзакция будет автоматически обработана из mempool в ledger
```

**Важные замечания:**
- **Ограничения команды `tx_sign`:**
  - Может добавлять подписи **только** к **арбитражным транзакциям** (должен быть TSD маркер)
  - Сертификаты должны принадлежать владельцам токена (проверяется по `auth_pkeys`)
  - Нельзя подписывать обычные (не-арбитражные) транзакции
- **Хеш транзакции меняется** после каждой операции `tx_sign`:
  - Старый хеш становится недействительным
  - Новый хеш должен использоваться для последующих операций
  - Используйте хеш, возвращенный в ответе `tx_sign` для следующей подписи
- **Поведение mempool:**
  - Транзакция остается в mempool пока не будет собрано достаточно подписей
  - Возвращает `DAP_LEDGER_CHECK_NOT_ENOUGH_VALID_SIGNS` (не ошибка - ожидаемое поведение)
  - Автоматически обрабатывается после достижения порога `auth_signs_valid`
- **Файлы сертификатов:**
  - Сертификаты должны быть сохранены в файлы в директории сертификатов
  - CLI ищет файлы `<cert_name>.dcert`, а не только сертификаты в памяти
  - Путь по умолчанию: `/opt/cellframe-node/etc/certs/` (или как настроено)

#### Команда `tx_sign`

Команда `tx_sign` позволяет добавлять подписи к существующим арбитражным транзакциям в mempool для распределенного подписания.

**Синтаксис:**
```bash
cellframe-node-cli tx_sign \
    -net <network_name> \
    -chain <chain_name> \
    -tx <transaction_hash> \
    -certs <cert1>[,<cert2>,...]
```

**Параметры:**
- `-net`: Имя сети
- `-chain`: Имя цепочки  
- `-tx`: Хеш транзакции (должна быть в mempool)
- `-certs`: Список имен сертификатов через запятую

**Важные ограничения:**
1. Может подписывать **ТОЛЬКО** арбитражные транзакции (должен быть TSD маркер `DAP_CHAIN_TX_TSD_TYPE_ARBITRAGE`)
2. Сертификаты должны принадлежать владельцам токена (проверяется по `auth_pkeys` токена)
3. Транзакция должна существовать в mempool (еще не обработана в ledger)
4. Нельзя подписывать обычные (не-арбитражные) транзакции

**Ответ:**
```json
{
  "status": "ok",
  "old_hash": "0xABC123...",
  "new_hash": "0xDEF456...",
  "signatures_added": 1,
  "total_signatures": 3
}
```

**Поведение изменения хеша:**
- Каждая операция `tx_sign` создает **НОВУЮ** транзакцию с дополнительными подписями
- Старый хеш транзакции становится **НЕДЕЙСТВИТЕЛЬНЫМ**
- Новый хеш ДОЛЖЕН использоваться для последующих операций `tx_sign`
- Это ожидаемое поведение для мульти-подписных транзакций

**Пример рабочего процесса:**

```bash
# Начальная транзакция (2 подписи: кошелек + cert1)
TX_HASH_1="0xABC123..."

# Добавление второй подписи (теперь 3 подписи всего)
RESULT=$(cellframe-node-cli tx_sign -net Backbone -tx $TX_HASH_1 -certs owner_cert2)
TX_HASH_2=$(echo $RESULT | jq -r '.new_hash')  # Извлечь новый хеш

# Добавление третьей подписи (теперь 4 подписи всего)
RESULT=$(cellframe-node-cli tx_sign -net Backbone -tx $TX_HASH_2 -certs owner_cert3)
TX_HASH_FINAL=$(echo $RESULT | jq -r '.new_hash')

# Транзакция автоматически обрабатывается в ledger после достижения порога auth_signs_valid
```

**Сообщения об ошибках:**
| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Transaction not found in mempool` | TX уже обработана или неверный хеш | Проверьте правильность хеша и что TX еще в mempool |
| `Transaction is not an arbitrage transaction` | Отсутствует TSD маркер | Можно подписывать только арбитражные TX - используйте флаг `-arbitrage` в `tx_create` |
| `None of the provided certificates belong to token owners` | Сертификаты не в `auth_pkeys` | Используйте сертификаты, которые были использованы для создания токена |
| `Arbitrage marker lost after adding signatures` | Внутренняя ошибка | Сообщите об ошибке - TSD маркер должен сохраняться |

**Тестирование арбитража с множественными подписями:**

```bash
# Шаг 1: Создание токена с требованием мульти-подписей
cellframe-node-cli token_decl \
    -net testnet \
    -token MULTISIG \
    -total_supply 100000.0 \
    -signs_valid 3 \  # Требуется 3 подписи
    -signs_total 3 \
    -certs owner1,owner2,owner3

# Шаг 2: Создание арбитражной TX с недостаточными подписями (распределенное подписание)
# ПРИМЕЧАНИЕ: Кошелек должен содержать оба токена (MULTISIG и native для комиссии)
cellframe-node-cli tx_create \
    -net testnet \
    -token MULTISIG \
    -from_wallet arb_wallet \
    -value 5000.0 \
    -fee 0.000000000000000001 \
    -arbitrage \
    -certs owner1  # Только 1 сертификат владельца (нужно 3 всего)
# Уведомление: "Добавьте оставшиеся 2 подписи через команду 'tx_sign'"
# Возвращает: hash1 (TX в mempool с 2 подписями: кошелек + owner1)

# Шаг 3: Добавление второй подписи владельца
cellframe-node-cli tx_sign \
    -net testnet \
    -chain main \
    -tx <hash1> \
    -certs owner2
# Возвращает: hash2 (теперь 3 подписи: кошелек + owner1 + owner2, еще 1 нужна)

# Шаг 4: Добавление третьей подписи владельца
cellframe-node-cli tx_sign \
    -net testnet \
    -chain main \
    -tx <hash2> \
    -certs owner3
# Возвращает: hash3 (теперь 4 подписи: кошелек + 3 владельца)
# TX автоматически обрабатывается в ledger (достигнут порог auth_signs_valid=3)
```

### Пример: Экстренное восстановление UTXO

```bash
# Сценарий: UTXO случайно заблокирован, нужно восстановить средства

# Шаг 1: Проверить адрес сетевых комиссий
cellframe-node-cli net get -net Backbone fee_addr
# Вывод: mAbCdEfGhIjKlMnOpQrStUvWxYz0123456789  (адрес сетевых комиссий)

# Шаг 2: Создать арбитражную транзакцию
# ПРИМЕЧАНИЕ: Выходы автоматически идут на адрес сетевых комиссий (net->pub.fee_addr)
cellframe-node-cli tx_create \
    -net Backbone \
    -token LOCKED \
    -from_wallet owner_wallet \
    -value 10000.0 \
    -arbitrage \
    -certs token_owner
# Параметр -to_addr не нужен - CLI автоматически использует mAbCdEfGhIjKlMnOpQrStUvWxYz0123456789

# Шаг 3: Проверить что транзакция создана
cellframe-node-cli tx_history -net Backbone -addr mAbCdEfGhIjKlMnOpQrStUvWxYz0123456789

# Шаг 4: С адреса сбора комиссий перераспределить средства нормально
cellframe-node-cli tx_create \
    -net Backbone \
    -token LOCKED \
    -from mAbCdEfGhIjKlMnOpQrStUvWxYz0123456789 \
    -to <rightful_owner_address> \
    -value 10000.0
```

### Лучшие практики

1. **Адрес сетевых комиссий** - Убедитесь что адрес сетевых комиссий настроен перед использованием арбитража
2. **Мульти-подписи** - Установите `auth_signs_valid` > 1 для требования множественных подписей владельцев
3. **Документация** - Ведите аудит-трейл всех арбитражных транзакций
4. **Тестирование** - Протестируйте арбитраж на тестнете перед использованием на мейннете

### Соображения безопасности

**Для владельцев токенов:**
- Используйте требование мульти-подписей когда возможно
- Документируйте все арбитражные транзакции
- Все средства арбитража идут на адрес сетевых комиссий - координируйтесь с операторами сети

**Для держателей токенов:**
- Проверьте установлен ли у токена `UTXO_ARBITRAGE_TX_DISABLED`
- Проверьте правильность настройки адреса сетевых комиссий
- Арбитраж не использует `tx_recv_allow` - средства идут только на адрес сетевых комиссий


## Заключение

Механизм блокировки UTXO в сочетании с арбитражными транзакциями обеспечивает детальный контроль над транзакциями токенов. Эмитенты токенов могут реализовать требования соответствия, безопасности и бизнес-логики, комбинируя:
- Блокировку UTXO
- Адресные ограничения
- Временную семантику (отложенная активация/разблокировка)
- Аварийное восстановление через арбитраж

Для дополнительной поддержки свяжитесь с командой разработки Cellframe.

